const env = {
  database: 'gobarber',
  username: 'root',
  password: 'root',
  host: 'localhost',
  dialect: 'mysql',
  define: {
    timestamps: true,
    underscored: true,
    underscoredAll: true,
  },
};

module.exports = env;
